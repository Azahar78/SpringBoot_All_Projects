spring:
  application:
    name: ORDER-SERVICE
  boot:
    admin:
      client:
        url: http://localhost:9999

server:
  port: 9090

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka		

management:
  endpoints:
    web:
      exposure:
        include: "*"